# CONST-035 baseline file.
# Captured: 2026-05-01 (sub-project 1 Phase 2, Yole main repo).
# Reducing this file is the work of campaign sub-projects 4-5.
# Section 1: static scanner hits, format <path>:<BLUFF-ID>.
# Section 2: per-file mutation kill rates, format <path>:<rate>:<mutants>.
#   Captured incrementally as sub-project 4 runs Pitest per module; empty
#   at sub-project 1 close.
# Section 3: anchor-manifest gaps, format <capability-id>:MISSING_ANCHOR.
# === SECTION 1: STATIC SCANNER HITS ===
# (empty — all BLUFF-K-011 sites resolved 2026-05-08: getOrNull + null check
#  + FAILED emission added to all 6 uploadFile sites across all 5 protocols)
# === SECTION 2: PER-FILE MUTATION KILL RATES ===
# (deferred to sub-project 4 — Pitest full-tree pass too slow for sub-project 1)
# === SECTION 3: ANCHOR-MANIFEST GAPS ===
# (empty — CAP-002 upgraded from pending-anchor to active in iter 23
#  by reusing the existing `test binary format registration` test in
#  BinaryDetectionTest.kt; the anchor verifier accepts Kotlin
#  backticked test names since iter 2.)
